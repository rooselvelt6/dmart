[package]
name = "dmart-server"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "dmart-server"
path = "src/main.rs"

[lib]
name = "dmart_server"
path = "src/lib.rs"

[dependencies]
dmart-shared = { path = "../dmart-shared" }

# Web framework
axum = { version = "0.8", features = ["multipart"] }
tokio = { version = "1", features = ["full"] }
tower = "0.5"
tower-http = { version = "0.6", features = ["cors", "fs", "trace", "set-header"] }
futures-util = "0.3"

# Database
surrealdb = { version = "2", features = ["kv-surrealkv"] }

# Cache
redis = { version = "0.27", features = ["aio", "tokio-comp"] }

# Serialization
serde = { workspace = true }
serde_json = { workspace = true }
chrono = { workspace = true }
uuid = { workspace = true }

# Export
printpdf = "0.7"
csv = "1.3"
qrcode = "0.14"
image = "0.25"

# Environment
dotenvy = "0.15"

# Error handling
thiserror = "2"
anyhow = "1"

# Logging
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "json", "fmt"] }
tracing-opentelemetry = "0.25"

# Metrics & Observability
metrics = "0.21"
metrics-exporter-prometheus = { version = "0.13", default-features = false, features = ["hyper", "tokio"] }
metrics-process = "2"

# OpenTelemetry (solo SDK para métricas/traces locales)
opentelemetry = { version = "0.25", features = ["metrics", "trace"] }
opentelemetry_sdk = { version = "0.25", features = ["metrics", "trace"] }
opentelemetry-semantic-conventions = "0.25"

# Health & Graceful Shutdown
axum-health = "0.1"

# Cryptography
chacha20poly1305 = "0.10"
rand = "0.8"
zeroize = { version = "1.7", features = ["derive"] }
base64 = "0.22"
sha2 = "0.10"

# JWT Authentication
jsonwebtoken = "9"

# TOTP multi-factor authentication
totp-rs = { version = "5", default-features = false, features = ["otpauth", "gen_secret"] }

# Argon2 password hashing (HIPAA compliant)
argon2 = "0.5"

# Async traits
async-trait = "0.1"

[dev-dependencies]
reqwest = { version = "0.12", default-features = false, features = ["json", "rustls-tls"] }
tempfile = "3"
tower = { version = "0.5", features = ["util"] }
http-body-util = "0.1"
totp-rs = { version = "5", default-features = false, features = ["otpauth"] }
proptest = "1.4"
